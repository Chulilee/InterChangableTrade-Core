import { BlockchainIndexerModule } from './blockchain-indexer.module';

describe('BlockchainIndexerModule', () => {
  it('should define the module', () => {
    expect(BlockchainIndexerModule).toBeDefined();
  });
});
